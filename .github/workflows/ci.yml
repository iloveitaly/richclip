name: CI

on:
  push:
    branches:
      - master
  pull_request:

jobs:
  check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - uses: jdx/mise-action@v3

      - name: Lint
        run: just lint

      - name: Test
        run: just test

      - name: Build
        run: just build
